let array = [];

// The `;` after the loop is an empty statement.
for(let i = 0; i < array.length; a[i++] = 0) /* empty */;

let a;

if ((a === 0) || (b === 0)); // Oops! This line does nothing...
    o = null;                // and this line is always executed.